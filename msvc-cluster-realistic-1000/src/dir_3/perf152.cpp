#include <Windows.h>
#include <vector>
#include <inc_9/header_185.h>
static_assert(sizeof(GenClass_185) > 0, "failed");
#include <inc_4/header_96.h>
static_assert(sizeof(GenClass_96) > 0, "failed");
#include <inc_8/header_168.h>
static_assert(sizeof(GenClass_168) > 0, "failed");
std::vector<int> perf_func_152() {
    LoadLibrary("abc.dll");
    return {152};
}
