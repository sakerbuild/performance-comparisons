#include <Windows.h>
#include <vector>
#include <inc_4/header_94.h>
static_assert(sizeof(GenClass_94) > 0, "failed");
std::vector<int> perf_func_136() {
    LoadLibrary("abc.dll");
    return {136};
}
