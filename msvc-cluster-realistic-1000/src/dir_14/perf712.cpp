#include <Windows.h>
#include <vector>
#include <inc_2/header_53.h>
static_assert(sizeof(GenClass_53) > 0, "failed");
#include <inc_8/header_168.h>
static_assert(sizeof(GenClass_168) > 0, "failed");
std::vector<int> perf_func_712() {
    LoadLibrary("abc.dll");
    return {712};
}
