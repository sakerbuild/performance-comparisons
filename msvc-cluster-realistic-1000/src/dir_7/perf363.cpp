#include <Windows.h>
#include <vector>
#include <inc_8/header_171.h>
static_assert(sizeof(GenClass_171) > 0, "failed");
#include <inc_7/header_158.h>
static_assert(sizeof(GenClass_158) > 0, "failed");
std::vector<int> perf_func_363() {
    LoadLibrary("abc.dll");
    return {363};
}
