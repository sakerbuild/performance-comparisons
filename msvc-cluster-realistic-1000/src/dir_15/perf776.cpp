#include <Windows.h>
#include <vector>
#include <inc_8/header_161.h>
static_assert(sizeof(GenClass_161) > 0, "failed");
std::vector<int> perf_func_776() {
    LoadLibrary("abc.dll");
    return {776};
}
