#include <Windows.h>
#include <vector>
#include <inc_8/header_165.h>
static_assert(sizeof(GenClass_165) > 0, "failed");
#include <inc_8/header_171.h>
static_assert(sizeof(GenClass_171) > 0, "failed");
std::vector<int> perf_func_929() {
    LoadLibrary("abc.dll");
    return {929};
}
