#include <Windows.h>
#include <vector>
#include <inc_8/header_176.h>
static_assert(sizeof(GenClass_176) > 0, "failed");
#include <inc_1/header_24.h>
static_assert(sizeof(GenClass_24) > 0, "failed");
std::vector<int> perf_func_947() {
    LoadLibrary("abc.dll");
    return {947};
}
