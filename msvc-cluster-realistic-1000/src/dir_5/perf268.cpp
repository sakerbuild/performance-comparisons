#include <Windows.h>
#include <vector>
#include <inc_4/header_87.h>
static_assert(sizeof(GenClass_87) > 0, "failed");
#include <inc_0/header_4.h>
static_assert(sizeof(GenClass_4) > 0, "failed");
#include <inc_9/header_185.h>
static_assert(sizeof(GenClass_185) > 0, "failed");
std::vector<int> perf_func_268() {
    LoadLibrary("abc.dll");
    return {268};
}
