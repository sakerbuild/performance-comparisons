#include <Windows.h>
#include <vector>
#include <inc_3/header_71.h>
static_assert(sizeof(GenClass_71) > 0, "failed");
#include <inc_7/header_148.h>
static_assert(sizeof(GenClass_148) > 0, "failed");
std::vector<int> perf_func_390() {
    LoadLibrary("abc.dll");
    return {390};
}
