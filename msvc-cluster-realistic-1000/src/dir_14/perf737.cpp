#include <Windows.h>
#include <vector>
#include <inc_6/header_129.h>
static_assert(sizeof(GenClass_129) > 0, "failed");
std::vector<int> perf_func_737() {
    LoadLibrary("abc.dll");
    return {737};
}
