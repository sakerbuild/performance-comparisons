#include <Windows.h>
#include <vector>
#include <inc_1/header_38.h>
static_assert(sizeof(GenClass_38) > 0, "failed");
#include <inc_5/header_101.h>
static_assert(sizeof(GenClass_101) > 0, "failed");
std::vector<int> perf_func_369() {
    LoadLibrary("abc.dll");
    return {369};
}
