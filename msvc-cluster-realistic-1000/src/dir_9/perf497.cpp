#include <Windows.h>
#include <vector>
#include <inc_0/header_18.h>
static_assert(sizeof(GenClass_18) > 0, "failed");
#include <inc_5/header_106.h>
static_assert(sizeof(GenClass_106) > 0, "failed");
std::vector<int> perf_func_497() {
    LoadLibrary("abc.dll");
    return {497};
}
