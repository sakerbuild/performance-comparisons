#include <Windows.h>
#include <vector>
#include <inc_1/header_24.h>
static_assert(sizeof(GenClass_24) > 0, "failed");
std::vector<int> perf_func_942() {
    LoadLibrary("abc.dll");
    return {942};
}
