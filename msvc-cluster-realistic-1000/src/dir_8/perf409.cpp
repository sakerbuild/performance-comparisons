#include <Windows.h>
#include <vector>
#include <inc_3/header_62.h>
static_assert(sizeof(GenClass_62) > 0, "failed");
#include <inc_3/header_67.h>
static_assert(sizeof(GenClass_67) > 0, "failed");
std::vector<int> perf_func_409() {
    LoadLibrary("abc.dll");
    return {409};
}
