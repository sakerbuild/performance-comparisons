#include <Windows.h>
#include <vector>
#include <inc_8/header_176.h>
static_assert(sizeof(GenClass_176) > 0, "failed");
std::vector<int> perf_func_442() {
    LoadLibrary("abc.dll");
    return {442};
}
