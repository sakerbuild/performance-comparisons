#include <Windows.h>
#include <vector>
#include <inc_7/header_153.h>
static_assert(sizeof(GenClass_153) > 0, "failed");
std::vector<int> perf_func_740() {
    LoadLibrary("abc.dll");
    return {740};
}
