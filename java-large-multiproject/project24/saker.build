build(out javac) {
$javac = saker.java.compile(
    SourceDirectories: src/main/java,
    Identifier: project24,
    ClassPath: [
        include(build, Path: "wd:/project11/saker.build")[javac],
    ],
)
}
