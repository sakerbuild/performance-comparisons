#include <Windows.h>
#include <vector>
#include <inc_3/header_71.h>
static_assert(sizeof(GenClass_71) > 0, "failed");
std::vector<int> perf_func_731() {
    LoadLibrary("abc.dll");
    return {731};
}
