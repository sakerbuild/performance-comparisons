#include <Windows.h>
#include <vector>
#include <inc_1/header_22.h>
static_assert(sizeof(GenClass_22) > 0, "failed");
#include <inc_4/header_96.h>
static_assert(sizeof(GenClass_96) > 0, "failed");
std::vector<int> perf_func_303() {
    LoadLibrary("abc.dll");
    return {303};
}
