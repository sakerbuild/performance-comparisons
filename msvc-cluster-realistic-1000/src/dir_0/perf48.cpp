#include <Windows.h>
#include <vector>
#include <inc_5/header_113.h>
static_assert(sizeof(GenClass_113) > 0, "failed");
std::vector<int> perf_func_48() {
    LoadLibrary("abc.dll");
    return {48};
}
