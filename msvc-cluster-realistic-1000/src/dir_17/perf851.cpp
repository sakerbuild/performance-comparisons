#include <Windows.h>
#include <vector>
#include <inc_2/header_45.h>
static_assert(sizeof(GenClass_45) > 0, "failed");
std::vector<int> perf_func_851() {
    LoadLibrary("abc.dll");
    return {851};
}
