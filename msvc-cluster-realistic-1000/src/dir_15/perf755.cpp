#include <Windows.h>
#include <vector>
#include <inc_4/header_97.h>
static_assert(sizeof(GenClass_97) > 0, "failed");
#include <inc_5/header_110.h>
static_assert(sizeof(GenClass_110) > 0, "failed");
std::vector<int> perf_func_755() {
    LoadLibrary("abc.dll");
    return {755};
}
