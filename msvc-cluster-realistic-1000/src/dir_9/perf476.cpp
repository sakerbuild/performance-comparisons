#include <Windows.h>
#include <vector>
#include <inc_8/header_167.h>
static_assert(sizeof(GenClass_167) > 0, "failed");
#include <inc_2/header_58.h>
static_assert(sizeof(GenClass_58) > 0, "failed");
std::vector<int> perf_func_476() {
    LoadLibrary("abc.dll");
    return {476};
}
