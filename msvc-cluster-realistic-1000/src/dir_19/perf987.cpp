#include <Windows.h>
#include <vector>
#include <inc_9/header_187.h>
static_assert(sizeof(GenClass_187) > 0, "failed");
#include <inc_0/header_13.h>
static_assert(sizeof(GenClass_13) > 0, "failed");
std::vector<int> perf_func_987() {
    LoadLibrary("abc.dll");
    return {987};
}
