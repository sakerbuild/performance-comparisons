#include <Windows.h>
#include <vector>
#include <inc_9/header_180.h>
static_assert(sizeof(GenClass_180) > 0, "failed");
std::vector<int> perf_func_547() {
    LoadLibrary("abc.dll");
    return {547};
}
