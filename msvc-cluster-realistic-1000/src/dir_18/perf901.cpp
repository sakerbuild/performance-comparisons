#include <Windows.h>
#include <vector>
#include <inc_1/header_27.h>
static_assert(sizeof(GenClass_27) > 0, "failed");
#include <inc_7/header_151.h>
static_assert(sizeof(GenClass_151) > 0, "failed");
std::vector<int> perf_func_901() {
    LoadLibrary("abc.dll");
    return {901};
}
