#include <Windows.h>
#include <vector>
#include <inc_6/header_137.h>
static_assert(sizeof(GenClass_137) > 0, "failed");
std::vector<int> perf_func_244() {
    LoadLibrary("abc.dll");
    return {244};
}
