#include <Windows.h>
#include <vector>
#include <inc_9/header_195.h>
static_assert(sizeof(GenClass_195) > 0, "failed");
std::vector<int> perf_func_793() {
    LoadLibrary("abc.dll");
    return {793};
}
