#include <Windows.h>
#include <vector>
#include <inc_6/header_123.h>
static_assert(sizeof(GenClass_123) > 0, "failed");
std::vector<int> perf_func_887() {
    LoadLibrary("abc.dll");
    return {887};
}
