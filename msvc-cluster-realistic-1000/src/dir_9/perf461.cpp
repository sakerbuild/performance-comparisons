#include <Windows.h>
#include <vector>
#include <inc_7/header_148.h>
static_assert(sizeof(GenClass_148) > 0, "failed");
#include <inc_5/header_102.h>
static_assert(sizeof(GenClass_102) > 0, "failed");
std::vector<int> perf_func_461() {
    LoadLibrary("abc.dll");
    return {461};
}
