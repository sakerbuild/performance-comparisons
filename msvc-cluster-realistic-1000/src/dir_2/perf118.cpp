#include <Windows.h>
#include <vector>
#include <inc_4/header_83.h>
static_assert(sizeof(GenClass_83) > 0, "failed");
#include <inc_0/header_1.h>
static_assert(sizeof(GenClass_1) > 0, "failed");
std::vector<int> perf_func_118() {
    LoadLibrary("abc.dll");
    return {118};
}
