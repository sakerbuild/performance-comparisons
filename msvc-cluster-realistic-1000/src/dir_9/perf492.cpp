#include <Windows.h>
#include <vector>
#include <inc_9/header_196.h>
static_assert(sizeof(GenClass_196) > 0, "failed");
#include <inc_5/header_102.h>
static_assert(sizeof(GenClass_102) > 0, "failed");
std::vector<int> perf_func_492() {
    LoadLibrary("abc.dll");
    return {492};
}
