#include <Windows.h>
#include <vector>
#include <inc_4/header_85.h>
static_assert(sizeof(GenClass_85) > 0, "failed");
#include <inc_0/header_18.h>
static_assert(sizeof(GenClass_18) > 0, "failed");
#include <inc_8/header_175.h>
static_assert(sizeof(GenClass_175) > 0, "failed");
std::vector<int> perf_func_437() {
    LoadLibrary("abc.dll");
    return {437};
}
