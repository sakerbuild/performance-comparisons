#include <Windows.h>
#include <vector>
#include <inc_5/header_115.h>
static_assert(sizeof(GenClass_115) > 0, "failed");
#include <inc_8/header_171.h>
static_assert(sizeof(GenClass_171) > 0, "failed");
std::vector<int> perf_func_895() {
    LoadLibrary("abc.dll");
    return {895};
}
