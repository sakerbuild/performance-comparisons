#include <Windows.h>
#include <vector>
#include <inc_5/header_107.h>
static_assert(sizeof(GenClass_107) > 0, "failed");
#include <inc_7/header_148.h>
static_assert(sizeof(GenClass_148) > 0, "failed");
std::vector<int> perf_func_847() {
    LoadLibrary("abc.dll");
    return {847};
}
