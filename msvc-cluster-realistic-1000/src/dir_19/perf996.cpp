#include <Windows.h>
#include <vector>
#include <inc_9/header_181.h>
static_assert(sizeof(GenClass_181) > 0, "failed");
std::vector<int> perf_func_996() {
    LoadLibrary("abc.dll");
    return {996};
}
