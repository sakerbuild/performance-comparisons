#include <Windows.h>
#include <vector>
#include <inc_8/header_171.h>
static_assert(sizeof(GenClass_171) > 0, "failed");
#include <inc_4/header_91.h>
static_assert(sizeof(GenClass_91) > 0, "failed");
std::vector<int> perf_func_546() {
    LoadLibrary("abc.dll");
    return {546};
}
