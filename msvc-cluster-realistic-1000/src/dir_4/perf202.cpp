#include <Windows.h>
#include <vector>
#include <inc_6/header_126.h>
static_assert(sizeof(GenClass_126) > 0, "failed");
#include <inc_8/header_166.h>
static_assert(sizeof(GenClass_166) > 0, "failed");
#include <inc_4/header_95.h>
static_assert(sizeof(GenClass_95) > 0, "failed");
std::vector<int> perf_func_202() {
    LoadLibrary("abc.dll");
    return {202};
}
