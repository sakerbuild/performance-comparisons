#include <Windows.h>
#include <vector>
#include <inc_1/header_22.h>
static_assert(sizeof(GenClass_22) > 0, "failed");
#include <inc_5/header_116.h>
static_assert(sizeof(GenClass_116) > 0, "failed");
std::vector<int> perf_func_115() {
    LoadLibrary("abc.dll");
    return {115};
}
