#include <Windows.h>
#include <vector>
#include <inc_7/header_157.h>
static_assert(sizeof(GenClass_157) > 0, "failed");
std::vector<int> perf_func_962() {
    LoadLibrary("abc.dll");
    return {962};
}
