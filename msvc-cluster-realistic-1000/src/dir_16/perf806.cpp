#include <Windows.h>
#include <vector>
#include <inc_3/header_77.h>
static_assert(sizeof(GenClass_77) > 0, "failed");
#include <inc_5/header_111.h>
static_assert(sizeof(GenClass_111) > 0, "failed");
std::vector<int> perf_func_806() {
    LoadLibrary("abc.dll");
    return {806};
}
