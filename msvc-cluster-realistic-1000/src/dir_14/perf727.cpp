#include <Windows.h>
#include <vector>
#include <inc_3/header_60.h>
static_assert(sizeof(GenClass_60) > 0, "failed");
std::vector<int> perf_func_727() {
    LoadLibrary("abc.dll");
    return {727};
}
