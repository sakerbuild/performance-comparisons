#include <Windows.h>
#include <vector>
#include <inc_1/header_26.h>
static_assert(sizeof(GenClass_26) > 0, "failed");
#include <inc_4/header_90.h>
static_assert(sizeof(GenClass_90) > 0, "failed");
std::vector<int> perf_func_904() {
    LoadLibrary("abc.dll");
    return {904};
}
