#include <Windows.h>
#include <vector>
#include <inc_0/header_17.h>
static_assert(sizeof(GenClass_17) > 0, "failed");
#include <inc_5/header_101.h>
static_assert(sizeof(GenClass_101) > 0, "failed");
std::vector<int> perf_func_172() {
    LoadLibrary("abc.dll");
    return {172};
}
