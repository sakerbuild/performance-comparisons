#include <Windows.h>
#include <vector>
#include <inc_2/header_50.h>
static_assert(sizeof(GenClass_50) > 0, "failed");
std::vector<int> perf_func_383() {
    LoadLibrary("abc.dll");
    return {383};
}
