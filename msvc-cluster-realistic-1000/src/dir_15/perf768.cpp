#include <Windows.h>
#include <vector>
#include <inc_6/header_130.h>
static_assert(sizeof(GenClass_130) > 0, "failed");
std::vector<int> perf_func_768() {
    LoadLibrary("abc.dll");
    return {768};
}
