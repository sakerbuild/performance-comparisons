#include <Windows.h>
#include <vector>
#include <inc_0/header_15.h>
static_assert(sizeof(GenClass_15) > 0, "failed");
std::vector<int> perf_func_228() {
    LoadLibrary("abc.dll");
    return {228};
}
