#include <Windows.h>
#include <vector>
#include <inc_8/header_164.h>
static_assert(sizeof(GenClass_164) > 0, "failed");
#include <inc_1/header_31.h>
static_assert(sizeof(GenClass_31) > 0, "failed");
std::vector<int> perf_func_983() {
    LoadLibrary("abc.dll");
    return {983};
}
