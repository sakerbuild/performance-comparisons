#include <Windows.h>
#include <vector>
#include <inc_0/header_18.h>
static_assert(sizeof(GenClass_18) > 0, "failed");
#include <inc_7/header_154.h>
static_assert(sizeof(GenClass_154) > 0, "failed");
#include <inc_4/header_93.h>
static_assert(sizeof(GenClass_93) > 0, "failed");
std::vector<int> perf_func_957() {
    LoadLibrary("abc.dll");
    return {957};
}
