#include <Windows.h>
#include <vector>
#include <inc_7/header_142.h>
static_assert(sizeof(GenClass_142) > 0, "failed");
std::vector<int> perf_func_719() {
    LoadLibrary("abc.dll");
    return {719};
}
