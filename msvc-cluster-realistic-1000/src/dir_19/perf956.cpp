#include <Windows.h>
#include <vector>
#include <inc_9/header_197.h>
static_assert(sizeof(GenClass_197) > 0, "failed");
#include <inc_1/header_32.h>
static_assert(sizeof(GenClass_32) > 0, "failed");
std::vector<int> perf_func_956() {
    LoadLibrary("abc.dll");
    return {956};
}
