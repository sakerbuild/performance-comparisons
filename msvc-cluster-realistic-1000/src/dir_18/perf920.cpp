#include <Windows.h>
#include <vector>
#include <inc_8/header_164.h>
static_assert(sizeof(GenClass_164) > 0, "failed");
std::vector<int> perf_func_920() {
    LoadLibrary("abc.dll");
    return {920};
}
