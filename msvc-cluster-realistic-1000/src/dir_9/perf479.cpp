#include <Windows.h>
#include <vector>
#include <inc_8/header_163.h>
static_assert(sizeof(GenClass_163) > 0, "failed");
#include <inc_0/header_2.h>
static_assert(sizeof(GenClass_2) > 0, "failed");
std::vector<int> perf_func_479() {
    LoadLibrary("abc.dll");
    return {479};
}
