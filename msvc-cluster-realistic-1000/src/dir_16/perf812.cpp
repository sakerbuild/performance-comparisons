#include <Windows.h>
#include <vector>
#include <inc_2/header_55.h>
static_assert(sizeof(GenClass_55) > 0, "failed");
#include <inc_8/header_179.h>
static_assert(sizeof(GenClass_179) > 0, "failed");
std::vector<int> perf_func_812() {
    LoadLibrary("abc.dll");
    return {812};
}
