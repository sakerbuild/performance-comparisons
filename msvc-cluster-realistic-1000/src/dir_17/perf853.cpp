#include <Windows.h>
#include <vector>
#include <inc_4/header_88.h>
static_assert(sizeof(GenClass_88) > 0, "failed");
std::vector<int> perf_func_853() {
    LoadLibrary("abc.dll");
    return {853};
}
