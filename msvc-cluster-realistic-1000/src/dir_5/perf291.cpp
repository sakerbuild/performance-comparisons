#include <Windows.h>
#include <vector>
#include <inc_8/header_175.h>
static_assert(sizeof(GenClass_175) > 0, "failed");
#include <inc_2/header_55.h>
static_assert(sizeof(GenClass_55) > 0, "failed");
#include <inc_0/header_11.h>
static_assert(sizeof(GenClass_11) > 0, "failed");
std::vector<int> perf_func_291() {
    LoadLibrary("abc.dll");
    return {291};
}
