#include <Windows.h>
#include <vector>
#include <inc_8/header_170.h>
static_assert(sizeof(GenClass_170) > 0, "failed");
std::vector<int> perf_func_96() {
    LoadLibrary("abc.dll");
    return {96};
}
