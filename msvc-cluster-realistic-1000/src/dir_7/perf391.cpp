#include <Windows.h>
#include <vector>
#include <inc_3/header_77.h>
static_assert(sizeof(GenClass_77) > 0, "failed");
#include <inc_2/header_45.h>
static_assert(sizeof(GenClass_45) > 0, "failed");
std::vector<int> perf_func_391() {
    LoadLibrary("abc.dll");
    return {391};
}
