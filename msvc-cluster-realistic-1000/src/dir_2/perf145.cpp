#include <Windows.h>
#include <vector>
#include <inc_2/header_49.h>
static_assert(sizeof(GenClass_49) > 0, "failed");
std::vector<int> perf_func_145() {
    LoadLibrary("abc.dll");
    return {145};
}
