#include <Windows.h>
#include <vector>
#include <inc_2/header_55.h>
static_assert(sizeof(GenClass_55) > 0, "failed");
std::vector<int> perf_func_482() {
    LoadLibrary("abc.dll");
    return {482};
}
