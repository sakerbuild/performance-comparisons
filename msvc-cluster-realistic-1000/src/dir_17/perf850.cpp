#include <Windows.h>
#include <vector>
#include <inc_5/header_103.h>
static_assert(sizeof(GenClass_103) > 0, "failed");
std::vector<int> perf_func_850() {
    LoadLibrary("abc.dll");
    return {850};
}
