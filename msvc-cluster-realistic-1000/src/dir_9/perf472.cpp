#include <Windows.h>
#include <vector>
#include <inc_8/header_177.h>
static_assert(sizeof(GenClass_177) > 0, "failed");
#include <inc_0/header_15.h>
static_assert(sizeof(GenClass_15) > 0, "failed");
std::vector<int> perf_func_472() {
    LoadLibrary("abc.dll");
    return {472};
}
