build(out javac) {
$javac = saker.java.compile(
    SourceDirectories: src/main/java,
    Identifier: project68,
    ClassPath: [
        include(build, Path: "wd:/project15/saker.build")[javac],
        include(build, Path: "wd:/project16/saker.build")[javac],
    ],
)
}
