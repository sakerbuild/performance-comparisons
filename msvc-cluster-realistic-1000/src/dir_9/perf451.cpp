#include <Windows.h>
#include <vector>
#include <inc_5/header_105.h>
static_assert(sizeof(GenClass_105) > 0, "failed");
#include <inc_4/header_90.h>
static_assert(sizeof(GenClass_90) > 0, "failed");
#include <inc_9/header_188.h>
static_assert(sizeof(GenClass_188) > 0, "failed");
std::vector<int> perf_func_451() {
    LoadLibrary("abc.dll");
    return {451};
}
