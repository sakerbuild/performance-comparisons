#include <Windows.h>
#include <vector>
#include <inc_0/header_0.h>
static_assert(sizeof(GenClass_0) > 0, "failed");
#include <inc_6/header_139.h>
static_assert(sizeof(GenClass_139) > 0, "failed");
std::vector<int> perf_func_180() {
    LoadLibrary("abc.dll");
    return {180};
}
