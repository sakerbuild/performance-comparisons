#include <Windows.h>
#include <vector>
#include <inc_8/header_174.h>
static_assert(sizeof(GenClass_174) > 0, "failed");
std::vector<int> perf_func_466() {
    LoadLibrary("abc.dll");
    return {466};
}
