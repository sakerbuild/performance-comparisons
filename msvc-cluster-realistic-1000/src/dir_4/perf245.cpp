#include <Windows.h>
#include <vector>
#include <inc_9/header_199.h>
static_assert(sizeof(GenClass_199) > 0, "failed");
#include <inc_1/header_22.h>
static_assert(sizeof(GenClass_22) > 0, "failed");
std::vector<int> perf_func_245() {
    LoadLibrary("abc.dll");
    return {245};
}
