#include <Windows.h>
#include <vector>
#include <inc_1/header_37.h>
static_assert(sizeof(GenClass_37) > 0, "failed");
#include <inc_6/header_128.h>
static_assert(sizeof(GenClass_128) > 0, "failed");
std::vector<int> perf_func_593() {
    LoadLibrary("abc.dll");
    return {593};
}
