#include <Windows.h>
#include <vector>
#include <inc_0/header_16.h>
static_assert(sizeof(GenClass_16) > 0, "failed");
#include <inc_4/header_87.h>
static_assert(sizeof(GenClass_87) > 0, "failed");
std::vector<int> perf_func_733() {
    LoadLibrary("abc.dll");
    return {733};
}
