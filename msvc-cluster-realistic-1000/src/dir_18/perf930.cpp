#include <Windows.h>
#include <vector>
#include <inc_1/header_39.h>
static_assert(sizeof(GenClass_39) > 0, "failed");
#include <inc_8/header_175.h>
static_assert(sizeof(GenClass_175) > 0, "failed");
std::vector<int> perf_func_930() {
    LoadLibrary("abc.dll");
    return {930};
}
