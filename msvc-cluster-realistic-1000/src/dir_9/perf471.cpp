#include <Windows.h>
#include <vector>
#include <inc_7/header_144.h>
static_assert(sizeof(GenClass_144) > 0, "failed");
std::vector<int> perf_func_471() {
    LoadLibrary("abc.dll");
    return {471};
}
