#include <Windows.h>
#include <vector>
#include <inc_5/header_107.h>
static_assert(sizeof(GenClass_107) > 0, "failed");
std::vector<int> perf_func_249() {
    LoadLibrary("abc.dll");
    return {249};
}
