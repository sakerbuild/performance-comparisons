#include <Windows.h>
#include <vector>
#include <inc_5/header_113.h>
static_assert(sizeof(GenClass_113) > 0, "failed");
#include <inc_3/header_63.h>
static_assert(sizeof(GenClass_63) > 0, "failed");
std::vector<int> perf_func_420() {
    LoadLibrary("abc.dll");
    return {420};
}
