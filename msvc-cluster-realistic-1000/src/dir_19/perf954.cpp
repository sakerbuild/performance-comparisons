#include <Windows.h>
#include <vector>
#include <inc_0/header_3.h>
static_assert(sizeof(GenClass_3) > 0, "failed");
#include <inc_9/header_181.h>
static_assert(sizeof(GenClass_181) > 0, "failed");
#include <inc_0/header_6.h>
static_assert(sizeof(GenClass_6) > 0, "failed");
std::vector<int> perf_func_954() {
    LoadLibrary("abc.dll");
    return {954};
}
