#include <Windows.h>
#include <vector>
#include <inc_1/header_28.h>
static_assert(sizeof(GenClass_28) > 0, "failed");
#include <inc_3/header_69.h>
static_assert(sizeof(GenClass_69) > 0, "failed");
std::vector<int> perf_func_157() {
    LoadLibrary("abc.dll");
    return {157};
}
