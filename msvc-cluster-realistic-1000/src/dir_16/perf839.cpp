#include <Windows.h>
#include <vector>
#include <inc_1/header_30.h>
static_assert(sizeof(GenClass_30) > 0, "failed");
std::vector<int> perf_func_839() {
    LoadLibrary("abc.dll");
    return {839};
}
