#include <Windows.h>
#include <vector>
#include <inc_6/header_135.h>
static_assert(sizeof(GenClass_135) > 0, "failed");
std::vector<int> perf_func_955() {
    LoadLibrary("abc.dll");
    return {955};
}
