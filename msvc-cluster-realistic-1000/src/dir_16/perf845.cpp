#include <Windows.h>
#include <vector>
#include <inc_6/header_136.h>
static_assert(sizeof(GenClass_136) > 0, "failed");
std::vector<int> perf_func_845() {
    LoadLibrary("abc.dll");
    return {845};
}
