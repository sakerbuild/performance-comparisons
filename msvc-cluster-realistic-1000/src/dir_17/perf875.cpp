#include <Windows.h>
#include <vector>
#include <inc_6/header_139.h>
static_assert(sizeof(GenClass_139) > 0, "failed");
std::vector<int> perf_func_875() {
    LoadLibrary("abc.dll");
    return {875};
}
