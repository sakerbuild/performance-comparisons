#include <Windows.h>
#include <vector>
#include <inc_0/header_13.h>
static_assert(sizeof(GenClass_13) > 0, "failed");
#include <inc_4/header_96.h>
static_assert(sizeof(GenClass_96) > 0, "failed");
#include <inc_8/header_175.h>
static_assert(sizeof(GenClass_175) > 0, "failed");
std::vector<int> perf_func_441() {
    LoadLibrary("abc.dll");
    return {441};
}
