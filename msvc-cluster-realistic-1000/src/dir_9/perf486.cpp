#include <Windows.h>
#include <vector>
#include <inc_9/header_192.h>
static_assert(sizeof(GenClass_192) > 0, "failed");
std::vector<int> perf_func_486() {
    LoadLibrary("abc.dll");
    return {486};
}
