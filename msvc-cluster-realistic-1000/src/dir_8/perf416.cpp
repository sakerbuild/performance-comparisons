#include <Windows.h>
#include <vector>
#include <inc_5/header_105.h>
static_assert(sizeof(GenClass_105) > 0, "failed");
std::vector<int> perf_func_416() {
    LoadLibrary("abc.dll");
    return {416};
}
