#include <Windows.h>
#include <vector>
#include <inc_6/header_127.h>
static_assert(sizeof(GenClass_127) > 0, "failed");
std::vector<int> perf_func_312() {
    LoadLibrary("abc.dll");
    return {312};
}
