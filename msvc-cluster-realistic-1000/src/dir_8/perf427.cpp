#include <Windows.h>
#include <vector>
#include <inc_1/header_36.h>
static_assert(sizeof(GenClass_36) > 0, "failed");
std::vector<int> perf_func_427() {
    LoadLibrary("abc.dll");
    return {427};
}
