#include <Windows.h>
#include <vector>
#include <inc_4/header_99.h>
static_assert(sizeof(GenClass_99) > 0, "failed");
std::vector<int> perf_func_326() {
    LoadLibrary("abc.dll");
    return {326};
}
