#include <Windows.h>
#include <vector>
#include <inc_9/header_193.h>
static_assert(sizeof(GenClass_193) > 0, "failed");
std::vector<int> perf_func_941() {
    LoadLibrary("abc.dll");
    return {941};
}
