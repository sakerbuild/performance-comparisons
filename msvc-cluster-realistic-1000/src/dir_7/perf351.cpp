#include <Windows.h>
#include <vector>
#include <inc_2/header_55.h>
static_assert(sizeof(GenClass_55) > 0, "failed");
#include <inc_6/header_131.h>
static_assert(sizeof(GenClass_131) > 0, "failed");
std::vector<int> perf_func_351() {
    LoadLibrary("abc.dll");
    return {351};
}
