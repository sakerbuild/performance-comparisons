#include <Windows.h>
#include <vector>
#include <inc_0/header_13.h>
static_assert(sizeof(GenClass_13) > 0, "failed");
std::vector<int> perf_func_317() {
    LoadLibrary("abc.dll");
    return {317};
}
