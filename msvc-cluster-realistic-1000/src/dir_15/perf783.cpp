#include <Windows.h>
#include <vector>
#include <inc_7/header_141.h>
static_assert(sizeof(GenClass_141) > 0, "failed");
std::vector<int> perf_func_783() {
    LoadLibrary("abc.dll");
    return {783};
}
