#include <Windows.h>
#include <vector>
#include <inc_6/header_125.h>
static_assert(sizeof(GenClass_125) > 0, "failed");
#include <inc_9/header_183.h>
static_assert(sizeof(GenClass_183) > 0, "failed");
std::vector<int> perf_func_766() {
    LoadLibrary("abc.dll");
    return {766};
}
