#include <Windows.h>
#include <vector>
#include <inc_1/header_36.h>
static_assert(sizeof(GenClass_36) > 0, "failed");
#include <inc_2/header_55.h>
static_assert(sizeof(GenClass_55) > 0, "failed");
std::vector<int> perf_func_277() {
    LoadLibrary("abc.dll");
    return {277};
}
