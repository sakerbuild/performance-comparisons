#include <Windows.h>
#include <vector>
#include <inc_2/header_44.h>
static_assert(sizeof(GenClass_44) > 0, "failed");
std::vector<int> perf_func_968() {
    LoadLibrary("abc.dll");
    return {968};
}
