#include <Windows.h>
#include <vector>
#include <inc_9/header_193.h>
static_assert(sizeof(GenClass_193) > 0, "failed");
#include <inc_8/header_179.h>
static_assert(sizeof(GenClass_179) > 0, "failed");
std::vector<int> perf_func_747() {
    LoadLibrary("abc.dll");
    return {747};
}
