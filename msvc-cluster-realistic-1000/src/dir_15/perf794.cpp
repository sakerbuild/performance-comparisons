#include <Windows.h>
#include <vector>
#include <inc_9/header_187.h>
static_assert(sizeof(GenClass_187) > 0, "failed");
#include <inc_1/header_20.h>
static_assert(sizeof(GenClass_20) > 0, "failed");
#include <inc_3/header_68.h>
static_assert(sizeof(GenClass_68) > 0, "failed");
std::vector<int> perf_func_794() {
    LoadLibrary("abc.dll");
    return {794};
}
