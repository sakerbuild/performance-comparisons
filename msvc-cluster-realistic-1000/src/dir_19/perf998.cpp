#include <Windows.h>
#include <vector>
#include <inc_7/header_155.h>
static_assert(sizeof(GenClass_155) > 0, "failed");
#include <inc_8/header_171.h>
static_assert(sizeof(GenClass_171) > 0, "failed");
#include <inc_1/header_28.h>
static_assert(sizeof(GenClass_28) > 0, "failed");
std::vector<int> perf_func_998() {
    LoadLibrary("abc.dll");
    return {998};
}
