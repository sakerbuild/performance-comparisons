#include <Windows.h>
#include <vector>
#include <inc_8/header_175.h>
static_assert(sizeof(GenClass_175) > 0, "failed");
#include <inc_7/header_157.h>
static_assert(sizeof(GenClass_157) > 0, "failed");
std::vector<int> perf_func_772() {
    LoadLibrary("abc.dll");
    return {772};
}
