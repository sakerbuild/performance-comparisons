#include <Windows.h>
#include <vector>
#include <inc_3/header_76.h>
static_assert(sizeof(GenClass_76) > 0, "failed");
std::vector<int> perf_func_708() {
    LoadLibrary("abc.dll");
    return {708};
}
