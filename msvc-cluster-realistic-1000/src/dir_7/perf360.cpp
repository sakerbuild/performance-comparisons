#include <Windows.h>
#include <vector>
#include <inc_4/header_93.h>
static_assert(sizeof(GenClass_93) > 0, "failed");
std::vector<int> perf_func_360() {
    LoadLibrary("abc.dll");
    return {360};
}
