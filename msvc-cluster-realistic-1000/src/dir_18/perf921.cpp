#include <Windows.h>
#include <vector>
#include <inc_7/header_152.h>
static_assert(sizeof(GenClass_152) > 0, "failed");
std::vector<int> perf_func_921() {
    LoadLibrary("abc.dll");
    return {921};
}
