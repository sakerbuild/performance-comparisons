#include <Windows.h>
#include <vector>
#include <inc_9/header_199.h>
static_assert(sizeof(GenClass_199) > 0, "failed");
#include <inc_3/header_67.h>
static_assert(sizeof(GenClass_67) > 0, "failed");
std::vector<int> perf_func_166() {
    LoadLibrary("abc.dll");
    return {166};
}
