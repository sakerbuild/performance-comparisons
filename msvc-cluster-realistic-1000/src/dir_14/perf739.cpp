#include <Windows.h>
#include <vector>
#include <inc_9/header_199.h>
static_assert(sizeof(GenClass_199) > 0, "failed");
#include <inc_1/header_36.h>
static_assert(sizeof(GenClass_36) > 0, "failed");
std::vector<int> perf_func_739() {
    LoadLibrary("abc.dll");
    return {739};
}
