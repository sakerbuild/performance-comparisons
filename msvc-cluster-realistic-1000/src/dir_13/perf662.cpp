#include <Windows.h>
#include <vector>
#include <inc_1/header_33.h>
static_assert(sizeof(GenClass_33) > 0, "failed");
#include <inc_7/header_144.h>
static_assert(sizeof(GenClass_144) > 0, "failed");
std::vector<int> perf_func_662() {
    LoadLibrary("abc.dll");
    return {662};
}
