#include <Windows.h>
#include <vector>
#include <inc_0/header_16.h>
static_assert(sizeof(GenClass_16) > 0, "failed");
#include <inc_3/header_71.h>
static_assert(sizeof(GenClass_71) > 0, "failed");
std::vector<int> perf_func_992() {
    LoadLibrary("abc.dll");
    return {992};
}
