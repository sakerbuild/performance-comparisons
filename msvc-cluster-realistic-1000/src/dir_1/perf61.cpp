#include <Windows.h>
#include <vector>
#include <inc_7/header_149.h>
static_assert(sizeof(GenClass_149) > 0, "failed");
std::vector<int> perf_func_61() {
    LoadLibrary("abc.dll");
    return {61};
}
