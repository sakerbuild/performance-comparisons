#include <Windows.h>
#include <vector>
#include <inc_4/header_84.h>
static_assert(sizeof(GenClass_84) > 0, "failed");
#include <inc_7/header_153.h>
static_assert(sizeof(GenClass_153) > 0, "failed");
#include <inc_7/header_149.h>
static_assert(sizeof(GenClass_149) > 0, "failed");
std::vector<int> perf_func_945() {
    LoadLibrary("abc.dll");
    return {945};
}
