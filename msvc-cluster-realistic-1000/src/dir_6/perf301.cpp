#include <Windows.h>
#include <vector>
#include <inc_1/header_28.h>
static_assert(sizeof(GenClass_28) > 0, "failed");
#include <inc_4/header_93.h>
static_assert(sizeof(GenClass_93) > 0, "failed");
std::vector<int> perf_func_301() {
    LoadLibrary("abc.dll");
    return {301};
}
