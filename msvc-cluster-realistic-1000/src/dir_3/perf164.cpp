#include <Windows.h>
#include <vector>
#include <inc_4/header_81.h>
static_assert(sizeof(GenClass_81) > 0, "failed");
#include <inc_2/header_53.h>
static_assert(sizeof(GenClass_53) > 0, "failed");
std::vector<int> perf_func_164() {
    LoadLibrary("abc.dll");
    return {164};
}
