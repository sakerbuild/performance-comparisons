#include <Windows.h>
#include <vector>
#include <inc_5/header_107.h>
static_assert(sizeof(GenClass_107) > 0, "failed");
#include <inc_7/header_149.h>
static_assert(sizeof(GenClass_149) > 0, "failed");
std::vector<int> perf_func_762() {
    LoadLibrary("abc.dll");
    return {762};
}
