#include <Windows.h>
#include <vector>
#include <inc_4/header_90.h>
static_assert(sizeof(GenClass_90) > 0, "failed");
#include <inc_0/header_12.h>
static_assert(sizeof(GenClass_12) > 0, "failed");
std::vector<int> perf_func_861() {
    LoadLibrary("abc.dll");
    return {861};
}
