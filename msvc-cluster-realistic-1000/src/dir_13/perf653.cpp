#include <Windows.h>
#include <vector>
#include <inc_8/header_179.h>
static_assert(sizeof(GenClass_179) > 0, "failed");
std::vector<int> perf_func_653() {
    LoadLibrary("abc.dll");
    return {653};
}
