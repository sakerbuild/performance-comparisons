#include <Windows.h>
#include <vector>
#include <inc_1/header_36.h>
static_assert(sizeof(GenClass_36) > 0, "failed");
#include <inc_4/header_86.h>
static_assert(sizeof(GenClass_86) > 0, "failed");
std::vector<int> perf_func_320() {
    LoadLibrary("abc.dll");
    return {320};
}
