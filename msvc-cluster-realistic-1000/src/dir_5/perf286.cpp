#include <Windows.h>
#include <vector>
#include <inc_0/header_8.h>
static_assert(sizeof(GenClass_8) > 0, "failed");
std::vector<int> perf_func_286() {
    LoadLibrary("abc.dll");
    return {286};
}
