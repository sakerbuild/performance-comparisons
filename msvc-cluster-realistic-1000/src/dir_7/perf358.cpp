#include <Windows.h>
#include <vector>
#include <inc_5/header_115.h>
static_assert(sizeof(GenClass_115) > 0, "failed");
#include <inc_4/header_84.h>
static_assert(sizeof(GenClass_84) > 0, "failed");
std::vector<int> perf_func_358() {
    LoadLibrary("abc.dll");
    return {358};
}
