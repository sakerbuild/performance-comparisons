#include <Windows.h>
#include <vector>
#include <inc_0/header_10.h>
static_assert(sizeof(GenClass_10) > 0, "failed");
std::vector<int> perf_func_106() {
    LoadLibrary("abc.dll");
    return {106};
}
