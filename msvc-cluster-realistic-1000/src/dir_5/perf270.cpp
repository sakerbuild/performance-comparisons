#include <Windows.h>
#include <vector>
#include <inc_9/header_187.h>
static_assert(sizeof(GenClass_187) > 0, "failed");
#include <inc_5/header_110.h>
static_assert(sizeof(GenClass_110) > 0, "failed");
std::vector<int> perf_func_270() {
    LoadLibrary("abc.dll");
    return {270};
}
