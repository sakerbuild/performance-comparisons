#include <Windows.h>
#include <vector>
#include <inc_9/header_191.h>
static_assert(sizeof(GenClass_191) > 0, "failed");
#include <inc_2/header_55.h>
static_assert(sizeof(GenClass_55) > 0, "failed");
std::vector<int> perf_func_975() {
    LoadLibrary("abc.dll");
    return {975};
}
