#include <Windows.h>
#include <vector>
#include <inc_2/header_58.h>
static_assert(sizeof(GenClass_58) > 0, "failed");
std::vector<int> perf_func_209() {
    LoadLibrary("abc.dll");
    return {209};
}
