#include <Windows.h>
#include <vector>
#include <inc_0/header_5.h>
static_assert(sizeof(GenClass_5) > 0, "failed");
std::vector<int> perf_func_734() {
    LoadLibrary("abc.dll");
    return {734};
}
