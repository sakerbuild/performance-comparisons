#include <Windows.h>
#include <vector>
#include <inc_3/header_73.h>
static_assert(sizeof(GenClass_73) > 0, "failed");
#include <inc_4/header_96.h>
static_assert(sizeof(GenClass_96) > 0, "failed");
#include <inc_9/header_192.h>
static_assert(sizeof(GenClass_192) > 0, "failed");
std::vector<int> perf_func_925() {
    LoadLibrary("abc.dll");
    return {925};
}
