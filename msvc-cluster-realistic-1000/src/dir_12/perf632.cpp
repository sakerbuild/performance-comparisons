#include <Windows.h>
#include <vector>
#include <inc_1/header_35.h>
static_assert(sizeof(GenClass_35) > 0, "failed");
std::vector<int> perf_func_632() {
    LoadLibrary("abc.dll");
    return {632};
}
