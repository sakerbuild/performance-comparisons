#include <Windows.h>
#include <vector>
#include <inc_4/header_99.h>
static_assert(sizeof(GenClass_99) > 0, "failed");
#include <inc_3/header_67.h>
static_assert(sizeof(GenClass_67) > 0, "failed");
std::vector<int> perf_func_385() {
    LoadLibrary("abc.dll");
    return {385};
}
