#include <Windows.h>
#include <vector>
#include <inc_4/header_87.h>
static_assert(sizeof(GenClass_87) > 0, "failed");
std::vector<int> perf_func_184() {
    LoadLibrary("abc.dll");
    return {184};
}
