#include <Windows.h>
#include <vector>
#include <inc_4/header_93.h>
static_assert(sizeof(GenClass_93) > 0, "failed");
#include <inc_8/header_172.h>
static_assert(sizeof(GenClass_172) > 0, "failed");
std::vector<int> perf_func_978() {
    LoadLibrary("abc.dll");
    return {978};
}
