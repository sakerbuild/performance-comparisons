#include <Windows.h>
#include <vector>
#include <inc_1/header_21.h>
static_assert(sizeof(GenClass_21) > 0, "failed");
#include <inc_7/header_149.h>
static_assert(sizeof(GenClass_149) > 0, "failed");
std::vector<int> perf_func_372() {
    LoadLibrary("abc.dll");
    return {372};
}
