#include <Windows.h>
#include <vector>
#include <inc_9/header_183.h>
static_assert(sizeof(GenClass_183) > 0, "failed");
#include <inc_8/header_160.h>
static_assert(sizeof(GenClass_160) > 0, "failed");
std::vector<int> perf_func_430() {
    LoadLibrary("abc.dll");
    return {430};
}
