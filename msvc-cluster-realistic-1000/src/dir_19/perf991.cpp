#include <Windows.h>
#include <vector>
#include <inc_5/header_110.h>
static_assert(sizeof(GenClass_110) > 0, "failed");
std::vector<int> perf_func_991() {
    LoadLibrary("abc.dll");
    return {991};
}
