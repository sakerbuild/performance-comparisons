#include <Windows.h>
#include <vector>
#include <inc_9/header_190.h>
static_assert(sizeof(GenClass_190) > 0, "failed");
std::vector<int> perf_func_250() {
    LoadLibrary("abc.dll");
    return {250};
}
