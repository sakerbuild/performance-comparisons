#include <Windows.h>
#include <vector>
#include <inc_7/header_158.h>
static_assert(sizeof(GenClass_158) > 0, "failed");
#include <inc_5/header_115.h>
static_assert(sizeof(GenClass_115) > 0, "failed");
#include <inc_0/header_9.h>
static_assert(sizeof(GenClass_9) > 0, "failed");
std::vector<int> perf_func_298() {
    LoadLibrary("abc.dll");
    return {298};
}
