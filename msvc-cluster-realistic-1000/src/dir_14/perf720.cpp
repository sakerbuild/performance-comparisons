#include <Windows.h>
#include <vector>
#include <inc_8/header_178.h>
static_assert(sizeof(GenClass_178) > 0, "failed");
#include <inc_6/header_134.h>
static_assert(sizeof(GenClass_134) > 0, "failed");
std::vector<int> perf_func_720() {
    LoadLibrary("abc.dll");
    return {720};
}
