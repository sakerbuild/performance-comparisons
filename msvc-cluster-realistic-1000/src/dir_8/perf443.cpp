#include <Windows.h>
#include <vector>
#include <inc_7/header_151.h>
static_assert(sizeof(GenClass_151) > 0, "failed");
std::vector<int> perf_func_443() {
    LoadLibrary("abc.dll");
    return {443};
}
