#include <Windows.h>
#include <vector>
#include <inc_8/header_175.h>
static_assert(sizeof(GenClass_175) > 0, "failed");
std::vector<int> perf_func_682() {
    LoadLibrary("abc.dll");
    return {682};
}
